# pnpm already isolates dependencies per workspace; keep the defaults and only
# relax peer strictness so a single mismatched peer range cannot block install.
strict-peer-dependencies=false
link-workspace-packages=true
prefer-workspace-packages=true
enable-pre-post-scripts=false
production=false
