/** Shared calendar OAuth constant for the web application. */

/** Name of the short-lived anti-CSRF cookie set during the consent flow. */
export const OAUTH_STATE_COOKIE = 'space_calendar_oauth_state';
export const OAUTH_STATE_MAX_AGE_SECONDS = 10 * 60;
